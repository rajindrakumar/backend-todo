export const generateLovePercent = () =>{
    return `${Math.floor(Math.random()*100)}%`;
    
    // or we can write 
    //return `${~~(Math.random()*100)}%`; "~~, Math.floor" it makes also remove absolute value 
    
    };